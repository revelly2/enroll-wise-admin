
export interface Student {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  studentId: string;
  contactNumber: string;
  course: string;
  section: string;
  registrationDate: string;
}

export interface Course {
  id: string;
  name: string;
}
